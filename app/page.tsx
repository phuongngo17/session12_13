import React from 'react'

export default function page() {
  return (
    <div>
      {/* 
       reactjs 
       cơ chế reder dữ liệu Reactjs
       Nextjs 
       hỗ trợ sửa reder dữ liệu ở phía client và phía server
       NEXTJS:
       1. server components: mặc định khi các e tạo trang
       2. client components: dùng "use client " khi nào để dùng:
       cần tương tác dữ liệu dùng(use State, useEffect, các sự kiện (onclick));
       */}
    </div>
  )
}
